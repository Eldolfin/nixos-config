{
  stylix = {
    enable = true;
    autoEnable = true;
    targets = {
      # I use gruber-darker instead
      helix.enable = false;
      # Currently generates invalid wezterm config
      wezterm.enable = false;
    };
  };
}
